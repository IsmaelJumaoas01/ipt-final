{
  "name": "user-management-system",
  "version": "1.0.0",
  "description": "User Management System with Angular Frontend and Node.js Backend",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install concurrently pm2 rimraf --legacy-peer-deps && cd backend && npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "clean:frontend": "cd frontend && npx rimraf node_modules package-lock.json .angular/cache dist",
    "install:angular": "cd frontend && npm install --legacy-peer-deps && node build.js",
    "build:frontend": "npm run clean:frontend && cd frontend && npm cache clean --force && npm install --legacy-peer-deps && cd .. && npm run install:angular",
    "build": "npm run install:all && npm run build:frontend",
    "start": "pm2 start ecosystem.config.js --env production"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "pm2": "^5.4.3",
    "rimraf": "^5.0.5"
  }
}
