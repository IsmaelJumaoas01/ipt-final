{
  "name": "user-management-system",
  "version": "1.0.0",
  "description": "User Management System with Angular Frontend and Node.js Backend",
  "scripts": {
    "install:all": "npm install concurrently --legacy-peer-deps && cd frontend && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps && cd ../backend && npm install --legacy-peer-deps",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "start": "cd backend && npm start",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "render:build": "npm run install:all && cd frontend && npm run build",
    "render:start": "cd backend && npm start"
  },
  "dependencies": {
    "concurrently": "8.2.2"
  }
} 
 
 
 
 